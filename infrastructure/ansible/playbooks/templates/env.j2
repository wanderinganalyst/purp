# Environment Configuration for Purp
FLASK_APP=app.py
FLASK_ENV={{ 'production' if environment == 'production' else 'development' }}
SECRET_KEY={{ lookup('password', '/dev/null length=32 chars=ascii_letters,digits') }}
DATABASE_URL=sqlite:///{{ app_dir }}/instance/purp.db
PORT={{ app_port }}
HOST=0.0.0.0
